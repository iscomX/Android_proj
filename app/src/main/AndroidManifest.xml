<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iscom.sharedprefs">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="is.com.MainActivity"
            android:label="@string/activity_principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="is.com.Currency_Converter" />
        <activity android:name="is.com.RandomGame" />
        <activity android:name="is.com.ConnectGame" />
        <activity android:name="is.com.Media" />
        <activity android:name="is.com.Test"></activity>
    </application>

</manifest>